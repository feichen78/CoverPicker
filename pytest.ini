[pytest]
minversion = 7.0
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short --strict-markers
markers =
    slow: 标记需要长时间运行的测试（如大量文件扫描）
    ui: 需要 Qt 事件循环的 UI 测试
    integration: 需要真实 FFmpeg 的集成测试