# CoverPicker 项目全局上下文 (CONTEXT.md)

## 1. 项目概述
CoverPicker 是一个专业的视频封面挑选与精修工具。核心目标是帮助用户从长视频中快速、精准地挑选出最佳画面，并支持无损导出。

## 2. 核心功能规范 (基于 PRODUCT.md)
- **6.1 视频分区浏览**: 视频按总时长自动均分 5 个 Segment (A/B/C/D/E)；总时长 < 5 分钟时合并为单一分区。支持状态视觉标记（已浏览、已收藏、已导出）。
- **6.2 Grid 候选浏览**: 进入 Segment 后生成候选截图，支持切换网格密度 (9/12/16/25)，选择状态持久保存。
- **6.3 收藏与锁定**: 支持收藏（优先推荐）、锁定（全局重采样不替换）。锁定帧全程保留。
- **6.4 Zoom 精修 (核心)**: 选中单张截图后，可分层缩小时间窗口渐进采样 (L1 ±2s, L2 ±8s, L3 跨分区, L4 全局重采样)。
- **6.5 图片与视频导出**: 支持单张高清保存、批量导出收藏截图、导出指定时间区间无损视频片段 (FFmpeg stream copy)。导出文件自动按视频名分目录，文件名自动递增防覆盖。

## 3. 技术架构
- **UI 框架**: Flet (Python)
- **核心引擎**: FFmpeg (截图、无损导出)
- **架构模式**: 状态驱动 UI (State-Driven UI)，核心逻辑与 UI 彻底解耦。
- **状态管理**: 全局 `StateManager` (基于 JSON 持久化)。

## 4. 目录结构规范
- `src/`: 核心业务逻辑 (state_manager, video_scanner, frame_generator, video_exporter, utils)
- `ui/`: Flet 界面层 (app.py, views/, components/)
- `main.py`: 程序绝对主入口
- `cache/`: 截图与临时文件存储目录