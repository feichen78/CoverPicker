# 📋 CoverPicker 项目进度日志

> **维护说明**：本文件用于记录项目开发进度。每次对话结束后，请将最新状态更新于此。  
> 当你开新对话时，只需将此文件内容完整发送给 AI，即可快速恢复上下文。

---

## 🏷️ 当前版本信息

- **版本号**：`v0.9.0`
- **版本名称**：核心交互预览版（Core Interaction Preview）
- **发布日期**：2026-07-07
- **状态**：✅ **稳定运行**（基础功能可用，无阻塞性 Bug）

---

## ✅ 已完成功能（本版本范围）

### 1. 基础架构与环境
- [x] Python 3.13 + Flet 0.85.3 桌面开发环境搭建
- [x] 项目目录结构规划（`src/`, `ui/views/`, `StillPic/`）
- [x] 修复 Flet 0.85.3 API 兼容性问题（`TextButton` 等控件适配）
- [x] 修复桌面版本地图片加载问题（去掉 `file:///` 协议，直接传绝对路径）

### 2. 首页视频扫描（HomeView）
- [x] 扫描指定目录（`Z:\`）下的视频文件（支持 `.mp4`, `.mkv`, `.avi` 等常见格式）
- [x] 以列表形式展示所有视频文件
- [x] 点击视频条目跳转至分区视图

### 3. 分区浏览与截图生成（SegmentView）
- [x] 自动获取视频时长，并依据产品规范（<5分钟合并，≥5分钟均分 5 段）生成 A/B/C/D/E 分区
- [x] 顶部显示 A/B/C/D/E 彩色标签，支持点击切换分区
- [x] 网格密度切换：支持 9/12/16/25 张截图布局（16 张为默认）
- [x] 调用 FFmpeg 异步提取指定时间点的关键帧
- [x] 图片网格显示，点击单张截图高亮选中

### 4. 导出功能
- [x] 自动在项目根目录创建 `StillPic/` 文件夹
- [x] 按视频名称创建子文件夹（例：`StillPic/视频名/`）
- [x] 导出选中的截图，文件命名规则：`cover_{时间戳}s.jpg`

---

## 🚧 待开发功能（按产品文档优先级排序）

### 第一阶段（核心效率工具）
- [ ] **收藏与锁定功能**（产品文档 6.3）：锁定满意截图，防止被后续重采样替换。
- [ ] **Zoom 精修（L1-L4）**（产品文档 6.4）：点击单张图后逐级缩小时间窗口精细采样。

### 第二阶段（NAS 级稳定与持久化）
- [ ] **状态持久化**（产品文档 7）：使用 SQLite 保存用户操作记录（已浏览、已收藏、已导出状态）。
- [ ] **缓存自动清理**：定期清理 `Temp` 文件夹中的临时截图文件。
- [ ] **全局视频片段导出**（产品文档 6.5）：使用 FFmpeg stream copy 无损导出 MP4 片段。

### 第三阶段（优化与兼容）
- [ ] 升级 `main.py` 启动代码，将弃用的 `ft.app()` 替换为 `ft.run()`。
- [ ] 支持用户自定义 NAS 路径（而非硬编码 `Z:\`）。
- [ ] 增加多线程加载进度条，提升大文件体验。

---

## 📁 当前项目结构
C:\Personal\CoverPicker
├── main.py # 程序入口（视图栈管理）
├── PROGRESS.md # 本文件（进度日志）
├── src/
│ └── video_scanner.py # 视频扫描引擎（含分段计算逻辑）
├── ui/
│ └── views/
│ ├── home_view.py # 首页视图（视频列表）
│ └── segment_view.py # 分区视图（分段 + Grid + 导出）【当前稳定版】
└── StillPic/ # 截图导出目录（自动生成）
└── [视频名]/
└── cover_xxx.jpg

---

## 🛠️ 技术栈摘要

| 组件     | 版本 / 说明                              |
| :------- | :--------------------------------------- |
| 编程语言 | Python 3.13                              |
| GUI 框架 | Flet 0.85.3（桌面版）                    |
| 视频处理 | FFmpeg / FFprobe（需系统 PATH 环境变量） |
| 异步并发 | asyncio                                  |
| 路径格式 | Windows 绝对路径（未使用 `file:///`）    |

---

## ⚠️ 已知限制与备注

1. **NAS 路径依赖**：目前硬编码为 `Z:\` 盘，非网络路径或移动硬盘需手动修改 `main.py`。
2. **导出覆盖风险**：若同一视频在相同秒数导出多次，会覆盖之前的文件（当前逻辑是直接覆写）。
3. **启动警告**：存在 `DeprecationWarning: app() is deprecated`，不影响运行，计划下次迭代修复。
4. **超时设置**：FFmpeg 截图单次超时设为 60 秒，超大 NAS 文件若网络延迟过高可能超时，需视情况调整。

---

## 📝 下次对话起点

如果你开启新对话，请直接粘贴本文件内容，并告诉我下一步想优先开发哪个功能（例如：“我想先做锁定功能”或“我想修复启动警告”）。

---
*最后更新时间：2026-07-08*
### 2026-07-08 更新 v0.10.0
- [x] **锁定功能**：支持锁定任意张截图（金色边框+锁图标），锁定后不会被“刷新未锁定”替换。
- [x] **刷新未锁定**：保留锁定截图，仅刷新未锁定部分，时间点随机偏移以提供新候选。
- [x] **全部重抽**：一键清空所有锁定，全部截图重新生成。
- [x] 底部工具栏新增三个操作按钮，交互反馈完善。
- [ ] # 📋 CoverPicker 项目进度日志

> **维护说明**：本文件记录项目开发进度。当你开启新对话时，请将本文件内容完整发送给 AI，即可快速恢复上下文。

---

## 🏷️ 当前版本信息

- **版本号**：`v0.10.24`
- **状态**：⚠️ **部分功能异常**（小视频截图失败，Zoom 弹窗待验证）
- **最后更新**：2026-07-08

---

## ⚠️ 当前核心问题

1. **小视频截图失败**：之前可以正常截图的小视频（57MB），现在过了很久才生成部分截图，有一些失败。
2. **Zoom 弹窗**：点击截图，没有弹窗出现。

**可能原因**：在迭代过程中修改了 `_extract_frame` 或 `GridView` 参数，影响了基础截图逻辑。

---

## ✅ 已完成功能

### 基础框架
- [x] Python 3.13 + Flet 0.85.3 桌面环境
- [x] 项目目录结构：`src/`, `ui/views/`, `StillPic/`
- [x] 视频扫描（`Z:\` 盘，14 个视频）

### 首页（HomeView）
- [x] 扫描并列出视频文件
- [x] 点击视频跳转至分区视图

### 分区视图（SegmentView）
- [x] A/B/C/D/E 五分区切换
- [x] 网格密度切换：9/12/16/25
- [x] FFmpeg 异步截图（支持大文件快速模式）
- [x] 点击选中截图（蓝色边框）
- [x] 锁定/解锁截图（金色边框 + 锁图标）
- [x] 刷新未锁定（保留锁定，重新生成其他）
- [x] 全部重抽（清空锁定，重新生成）
- [x] 导出选中截图到 `StillPic/` 目录
- [x] 自适应网格布局（3/3/4/5 列）

### Zoom 精修（ZoomDialog）
- [x] 双击截图触发 Zoom 弹窗（代码已集成）
- [x] L1 层级（±2s，9 张）
- [x] L2 层级（±8s，12 张，按钮切换）
- [x] 导出 Zoom 选中图片
- [ ] **状态**：弹窗已出现但功能待验证（因截图功能异常）

---

## 🚧 待解决问题

### 高优先级（阻塞）
1. **修复小视频截图失败**：回归到能正常工作的版本（如 v0.10.18），再逐步合并 Zoom 功能。
2. **验证 Zoom 弹窗**：在截图功能恢复后，测试双击触发、L2 切换、导出功能。

### 中优先级
1. **大文件超时优化**：当前 60 秒超时对 7GB+ 文件仍有部分失败，可考虑动态超时。
2. **进度提示**：大文件截图时界面无进度条，用户可能误以为卡死。

### 低优先级
1. **L3/L4 层级**（产品文档要求）
2. **状态持久化**（SQLite 保存收藏记录）
3. **FFmpeg 错误诊断**：更友好的错误提示

---

## 📁 当前项目结构
C:\Personal\CoverPicker
├── main.py # 程序入口
├── PROGRESS.md # 本文件
├── src/
│ └── video_scanner.py # 视频扫描引擎
├── ui/
│ └── views/
│ ├── home_view.py # 首页视图
│ ├── segment_view.py # 分区视图（v0.10.24）
│ └── zoom_dialog.py # Zoom 弹窗（v4）
└── StillPic/ # 导出目录

---

## 🛠️ 技术栈

| 组件   | 版本        |
| :----- | :---------- |
| Python | 3.13        |
| Flet   | 0.85.3      |
| FFmpeg | 需系统 PATH |
| 异步库 | asyncio     |

---

## 📝 建议下一步行动

### 选项 A：回退到稳定版本
- 回退 `segment_view.py` 到 **v0.10.18**（该版本已验证大文件 9/9 生成，小文件 8/9 生成）。
- 在新对话中逐步重新集成 Zoom 功能（逐小步测试）。

### 选项 B：基于当前版本修复
- 在当前对话中继续调试，定位小视频截图失败的具体原因。
- 但当前对话已较长，调试效率可能降低。

### 选项 C：新开对话 + 提供 PROGRESS.md
- 新对话中先提供本文件，让 AI 快速了解现状。
- 重点解决“小视频截图失败”和“Zoom 弹窗验证”两个问题。

---

## 📌 关键代码位置

- **截图函数**：`segment_view.py` 中的 `_extract_frame()`
- **网格刷新**：`segment_view.py` 中的 `_refresh_grid()`
- **Zoom 触发**：`segment_view.py` 中的 `_open_zoom()`
- **Zoom 弹窗**：`zoom_dialog.py` 中的 `ZoomDialog`

---

*最后更新：2026-07-08*
# 📋 CoverPicker 项目进度日志

> **维护说明**：本文件用于记录项目开发进度。每次对话结束后，请将最新状态更新于此。  
> 当你开启新对话时，只需将此文件内容完整发送给 AI，即可快速恢复上下文。

---

## 🏷️ 当前版本信息

- **版本号**：`v0.11.0`
- **版本名称**：架构重构预览版（Architecture Refactor Preview）
- **状态**：⚠️ **进行中 - UI 渲染问题待解决**
- **最后更新**：2026-07-08

---

## 🎯 当前目标

将应用从复杂的 `ft.View` + 视图栈架构，迁移到 **单页面模式（SPA）**，解决 Flet 0.85.3 在 Windows 上的渲染问题。

---

## ✅ 已完成工作

### 1. 问题诊断
- ✅ 确认问题根源：`ft.View` + 视图栈 + `GridView` 在 Flet 0.85.3 Windows 环境下存在渲染 bug
- ✅ 验证方案：最小化测试页面（蓝色背景）能正常显示 → 排除环境问题
- ✅ 确认问题在复杂布局中的某个控件

### 2. 架构重构
- ✅ 将 `main.py` 从视图栈模式改为单页面模式（`page.controls.clear()` + `page.add()`）
- ✅ 将 `SegmentView` 从 `ft.View` 改为 `ft.Container`
- ✅ 简化 UI 布局：去掉左侧视频列表，顶部合并为一行

### 3. 网格方案迭代
- ✅ 尝试 `Wrap` 方案 → 图片挤在左半边，不满足要求
- ✅ 尝试 `GridView` + `expand=True` → 充分利用屏幕空间
- ✅ 修复 `self.page` 属性冲突 → 改用 `self.main_page`

### 4. 分段测试（逐步恢复 UI）
- ✅ 阶段1：顶部栏 + 简单内容 → 正常显示
- ✅ 阶段2：+ 分段标签 → 正常显示
- ✅ 阶段3：+ 密度按钮 → 正常显示
- ✅ 阶段4：+ 底部操作栏 → 正常显示
- ✅ 阶段5：+ 网格占位（9个灰色方块）→ 正常显示

---

## ❌ 当前问题

**最终集成后，窗口变成灰色，无任何内容显示。**

- 分段测试中每个阶段都正常
- 但将所有组件组合后，整个 `SegmentView` 不渲染
- 控制台显示所有异步任务正常完成（截图成功）

**可能原因：**
1. `GridView` 与 `Column` 的 `expand` 属性组合导致布局计算失败
2. `GridView` 在 `Container` 嵌套时渲染异常
3. Flet 0.85.3 对 `GridView` + 动态内容更新的支持不稳定

---

## 📁 当前项目结构
C:\CoverPicker
├── main.py # 单页面模式入口
├── PROGRESS.md # 本文件
├── src/
│ └── video_scanner.py # 视频扫描引擎
├── ui/
│ └── views/
│ ├── home_view.py # 首页视图
│ └── segment_view.py # 分区视图（v5.2，当前待修复）
└── StillPic/ # 截图导出目录

---

## 🛠️ 技术栈

| 组件     | 版本 / 说明                              |
| :------- | :--------------------------------------- |
| 编程语言 | Python 3.13                              |
| GUI 框架 | Flet 0.85.3（桌面版）                    |
| 视频处理 | FFmpeg / FFprobe（需系统 PATH 环境变量） |
| 异步并发 | asyncio                                  |

---

## 🔧 下次调试起点

### 方案 A：继续 Flet 调试
1. 将 `grid_view` 替换为 `Wrap` + 手动计算宽度
2. 或将 `GridView` 移到 `Column` 外部，使用绝对定位
3. 或使用 `Row` + 循环手动构建网格

### 方案 B：切换技术栈（激进）
1. 改用 **PySide6**（Qt for Python），Windows 原生渲染，稳定可靠
2. 需要重写所有 UI 代码，但视频处理逻辑可复用

### 方案 C：升级 Flet
1. 升级到 Flet 最新版（0.95+）
2. 可能修复了 `GridView` 的渲染问题
3. 但可能存在新 API 兼容性问题

---

## 📝 关键代码位置

| 文件                       | 说明                                        |
| -------------------------- | ------------------------------------------- |
| `main.py`                  | 单页面模式入口，切换 HomeView ↔ SegmentView |
| `ui/views/segment_view.py` | 当前问题文件，v5.2 版本                     |
| `ui/views/home_view.py`    | 首页，视频列表扫描                          |

---

## 📌 建议

**下次继续时，优先尝试：**
1. 将 `grid_view` 替换为 `Wrap` + 手动设置每个卡片宽度，利用窗口宽度计算
2. 或者先在 `SegmentView` 中移除 `GridView`，用简单文本确认布局正常，再逐步添加

---

*最后更新时间：2026-07-08*
# 📋 CoverPicker 项目进度日志

> **维护说明**：本文件用于记录项目开发进度。每次对话结束后，请将最新状态更新于此。  
> 当你开启新对话时，只需将此文件内容完整发送给 AI，即可快速恢复上下文。

---

## 🏷️ 当前版本信息

- **版本号**：`v0.11.0`
- **版本名称**：架构重构尝试版（Architecture Refactor Attempt）
- **状态**：❌ **Flet 版本阻塞，决定迁移到 PySide6**
- **最后更新**：2026-07-09

---

## 🎯 当前目标

**将 CoverPicker 从 Flet 迁移到 PySide6（Qt for Python）**，以解决 Flet 0.85.3 在 Windows 桌面端的渲染 bug。

---

## ❌ 阻塞性结论

**Flet 0.85.3 在 Windows 上存在无法绕过的渲染 bug：**

- `GridView` 单独渲染 → ✅ 正常
- `GridView` + 任何布局容器（`Column`、`Stack`、`Container`）→ ❌ 灰色窗口
- 已尝试所有 Flet 布局方案（`Column`、`Stack`、`Wrap`、`Container` 嵌套）均无效
- 问题根源在 Flet 框架层，非代码逻辑问题
- 继续在 Flet 上投入时间收益极低，决定**切换技术栈**

---

## ✅ 今天已完成的工作（2026-07-09）

### 1. 系统性问题诊断
- ✅ 通过逐步简化 UI（5个阶段），确认了每个单独组件都能正常显示
- ✅ 定位到核心问题：`GridView` 与任何父布局容器组合时，整个页面不渲染
- ✅ 确认了 Flet 0.85.3 Windows 桌面端的渲染 bug 无法绕过

### 2. 技术决策
- ✅ 评估了继续 Flet 调试与切换 PySide6 的性价比
- ✅ 决定迁移到 PySide6（Qt for Python）

### 3. 迁移准备
- ✅ 明确迁移范围：UI 层重写，业务逻辑层（视频扫描、FFmpeg 截图）完全复用
- ✅ 预估迁移工作量：2-3 小时

---

## ✅ 已验证可用的功能（迁移后需保留）

### 视频处理层（`src/video_scanner.py`）— 完全复用
- [x] 扫描 `Z:\` 目录下的视频文件（`.mp4`, `.mkv`, `.avi` 等 11 种格式）
- [x] 获取视频时长（通过 FFprobe）
- [x] 计算 A/B/C/D/E 分段（<5分钟合并，≥5分钟均分5段）
- [x] 从 `Temp` 目录提取临时截图（通过 FFmpeg）

### UI 功能（迁移后需重写）
- [x] 首页视频列表展示
- [x] 分段标签切换（A/B/C/D/E）
- [x] 网格密度切换（9/12/16/25）
- [x] 截图网格展示（点击选中，蓝色边框高亮）
- [x] 锁定/解锁功能（金色边框 + 锁图标）
- [x] 刷新未锁定（保留锁定，重新生成其他）
- [x] 全部重抽（清空锁定，重新生成）
- [x] 导出选中截图到 `StillPic/` 目录
- [x] Zoom 精修弹窗（双击截图触发，L1/L2 层级）

---

## 📁 当前项目结构（迁移后保持不变）
C:\CoverPicker
├── main.py # 入口（待重写为 PySide6）
├── PROGRESS.md # 本文件
├── src/
│ └── video_scanner.py # ✅ 完全复用（视频扫描 + FFmpeg 调用）
├── ui/
│ └── views/
│ ├── home_view.py # ❌ 待重写（PySide6）
│ ├── segment_view.py # ❌ 待重写（PySide6）
│ └── zoom_dialog.py # ❌ 待重写（PySide6）
└── StillPic/ # 截图导出目录

---

## 🛠️ 新技术栈（PySide6）

| 组件     | 版本 / 说明                              |
| :------- | :--------------------------------------- |
| 编程语言 | Python 3.13                              |
| GUI 框架 | **PySide6**（Qt for Python，官方稳定版） |
| 视频处理 | FFmpeg / FFprobe（需系统 PATH）          |
| 异步并发 | asyncio（保持不变）                      |
| 布局系统 | QGridLayout / QVBoxLayout / QHBoxLayout  |
| 图片控件 | QLabel + QPixmap（替代 Image）           |

---

## 📋 迁移任务清单

### 第一阶段：基础框架
- [ ] 安装 PySide6：`pip install pyside6`
- [ ] 创建 `QApplication` + 主窗口（`QMainWindow`）
- [ ] 实现首页视频列表（`QListWidget` 或 `QListView`）

### 第二阶段：核心功能
- [ ] 实现分区视图（`QWidget` + `QGridLayout`）
- [ ] 实现分段标签（`QPushButton` 或 `QLabel` + 点击事件）
- [ ] 实现截图网格（`QGridLayout` 动态添加 `QLabel` + `QPixmap`）
- [ ] 实现密度切换（`QButtonGroup` + `QPushButton`）

### 第三阶段：交互功能
- [ ] 实现点击选中 + 蓝色边框（`QLabel` 样式切换）
- [ ] 实现锁定/解锁（金色边框 + 锁图标）
- [ ] 实现刷新未锁定 + 全部重抽
- [ ] 实现导出截图

### 第四阶段：Zoom 弹窗
- [ ] 实现 Zoom 精修弹窗（`QDialog`）
- [ ] L1/L2 层级切换
- [ ] 导出选中 Zoom 截图

### 第五阶段：优化
- [ ] 异步截图（`asyncio` + `QThread` 或 `QTimer`）
- [ ] 进度提示（`QProgressBar`）
- [ ] 清理临时文件

---

## 📌 关键可复用代码位置

| 文件                   | 说明                                                  |
| ---------------------- | ----------------------------------------------------- |
| `src/video_scanner.py` | **完全复用**：`scan_videos()`, `calculate_segments()` |
| `src/video_scanner.py` | **完全复用**：`_extract_frame()`（FFmpeg 截图逻辑）   |
| `StillPic/`            | 截图导出目录，逻辑不变                                |

---

## ⚠️ 注意事项

1. **PySide6 许可证**：LGPL，商业使用需遵守相应条款
2. **异步兼容**：PySide6 的 `QApplication.exec()` 会阻塞主线程，`asyncio` 事件循环需要与 Qt 事件循环配合（可用 `qasync` 或 `QTimer`）
3. **路径格式**：Windows 路径直接使用字符串，无需 `file:///` 前缀

---

## 📝 下次对话起点

**新对话中直接粘贴本文件，然后说：**

> “我已决定切换到 PySide6，请帮我开始迁移。先实现首页视频列表。”

---

*最后更新时间：2026-07-09*