# 📋 CoverPicker 项目进度日志

## 🏷️ 当前版本信息

| 项目                 | 内容                         |
| -------------------- | ---------------------------- |
| 版本号               | v3.0.1                       |
| 版本名称             | 信息区优化 + 放大预览自适应版 |
| 发布日期             | 2026-07-26                   |
| 状态                 | ✅ 核心功能稳定，已知问题已记录 |
| 稳定基准版本         | v2.5.2                       |
| 对应 PRODUCT.md 版本 | v6.4                         |


## ✅ v3.0.1 已实现功能

### 1. 主界面显示版本号
- 新增 `src/version.py`，定义 `VERSION = "3.0.1"`
- 主窗口标题显示 `CoverPicker - v3.0.1`
- 启动日志显示版本号

### 2. 左侧信息区优化
- 从 4 行改为 5 行：文件名 / 时长 / 分辨率 / 文件大小 / 路径
- 全部字体统一为 **Arial 12 Bold**
- 行间距设为 **6px**
- 顶部对齐，底部 `addStretch()` 吸收多余空间
- 路径使用 `QTextEdit`（保留 v2.5.2 修复），完整显示换行路径
- 视频列表拉伸因子从 2 改为 3，信息区保持拉伸因子 1

### 3. 分辨率自动获取
- 新增 `get_video_resolution()` 函数，用 ffprobe 获取视频宽高
- 导入视频时自动获取并存入数据库 `videos.resolution` 字段
- 在左侧信息区第 3 行显示

### 4. 放大预览窗口自适应缩放
- 在 `showEvent` 中延迟 50ms 计算初始缩放
- `fit_scale = min(视口宽度/图片宽度, 视口高度/图片高度)`
- 确保图片首次打开时适应窗口
- 无上限限制，最大不超过 `max_scale=5.0`

### 5. 放大预览窗口键盘方向键平移
- `←/→` 水平 ±20px
- `↑/↓` 垂直 ±20px
- ESC 关闭窗口

### 6. 收藏夹显示修复
- 恢复最大化/最小化按钮
- 图片缩放适应显示，保持宽高比

### 7. 代码优化
- `print` 调试语句改为 `logger.debug()`
- `refresh_unlocked` 和 `reset_segment` 增加边界检查
- `info_path` 的 `QTextEdit` 设置 `setMinimumHeight(20)` + `setSizePolicy(Minimum)`
- 修复退出时 qasync 事件循环关闭报错


## ⚠️ 已知问题

| 编号 | 问题描述 | 状态 | 影响 | 计划 |
|------|----------|------|------|------|
| P1 | main 分支快速切换视频时出现 `RuntimeError: Cannot enter into task...` | ⏳ 已定位，暂搁置 | 不影响功能，控制台有报错。v2.5.2 无此问题，v3.0.1 引入。经过逐文件排查，单个文件改动均不是原因，可能由多个改动组合效应导致。 | 暂搁置，待后续深入排查 |
| P2 | 排查过程中发现 `.ts` 文件在快速切换时偶尔卡住 | ⏳ 待进一步观察 | 偶发，可能是 FFmpeg 处理 MPEG-TS 格式的 seek 操作较慢 | 暂搁置 |


## 🔍 RuntimeError 排查记录

### 排查过程

| 步骤 | 操作 | 结果 | 结论 |
|------|------|------|------|
| 1 | 完全恢复 v2.5.2 | ✅ 无报错 | v2.5.2 是稳定基准 |
| 2 | 逐文件恢复 v3.0.1 改动 | 见下 | 见下 |
| 2a | 恢复 `favorites_dialog.py` | ❌ 报错仍在 | 收藏夹不是原因 |
| 2b | 恢复 `main.py` | ❌ 报错仍在 | main.py 不是原因 |
| 2c | 恢复 `image_labels.py` | ❌ 报错仍在 | image_labels.py 不是原因 |
| 2d | 恢复 `segment_controller.py` + `segment_view.py` 一起 | ✅ RuntimeError 消失 | 问题与这两个文件相关 |
| 2e | 细化排查 `segment_view.py` 改动 | 见下 | 见下 |
| 2e-i | `info_path` QTextEdit → QLabel | ❌ 报错仍在 | QTextEdit 不是原因 |
| 2e-ii | 移除 `addStretch()` | ❌ 报错仍在 | addStretch 不是原因 |
| 2e-iii | 移除分辨率行（4行结构） | ❌ 报错仍在 | 分辨率行不是原因 |
| 2e-iv | 拉伸因子 3→2 | ❌ 报错仍在 | 拉伸因子不是原因 |
| 2f | 恢复 `segment_controller.py` 到 v2.5.2（在测试5基础上） | ⚠️ 累积性报错 | 单个文件不是原因 |
| 2g | 清空缓存后测试 | ❌ 报错仍在 | 缓存不是原因 |

### 排查结论

- **v3.0.1 的每个单独改动都不是 RuntimeError 的直接原因**
- **RuntimeError 可能是多个改动的组合效应导致的**
- **报错不影响程序功能**（截图正常显示，视频正常切换，不崩溃）
- **排查成本已较高，决定暂时搁置**

### 后续计划

- 暂不修复此 RuntimeError
- 记录为已知问题
- 继续开发 v3.2 功能


## 🔴 已修复问题（v2.5.2）

### 问题4：长视频路径显示不完整 —— ✅ 已修复

**现象**：特定长文件名视频的路径在左侧信息区显示不完整，末尾被截断；在不同缩放比例下表现不一致。

**根本原因**：
1. `QLabel` 的 `wordWrap` 在包含反斜杠 `\` 的长字符串中无法正确计算所需总高度
2. 即使给予足够空间，`QLabel` 也不一定显示所有行（Qt/QLabel 的已知行为限制）
3. 信息组的垂直拉伸因子为0，无法随窗口扩展

**修复方案**：
1. 将 `info_path` 从 `QLabel` 替换为 `QTextEdit`，配置为只读、无边框、背景透明
2. `QTextEdit` 使用完整的文档引擎，能正确计算每行文本高度并自动调整
3. 信息组拉伸因子从0改为1，使其能随窗口垂直扩展


### 问题8：视频状态图标优先级错误 —— ✅ 已修复

**现象**：视频既有收藏（⭐）又有导出（✅）时，列表显示⭐而非✅。

**根本原因**：`get_video_state_icon` 中优先级顺序为 ⭐ > ✅ > 👁️，与需求不符。

**修复方案**：将优先级改为 ✅ > ⭐ > 👁️（符合 PRODUCT.md 7.1）。


### 问题9：NAS收藏静默回退到临时目录 —— ✅ 已修复

**现象**：NAS路径不可写时，收藏截图静默保存到临时目录，用户无感知。

**根本原因**：`_save_favorite_to_nas` 方法在保存失败时直接返回临时路径，未向用户提示。

**修复方案**：
1. `_save_favorite_to_nas` 返回值从 `str` 改为 `Tuple[str, bool]`，返回路径和是否成功保存到NAS
2. `favorite_selected` 中记录失败次数，通过 `_notify_progress` 向用户显示提示


### 新增修复：细选窗口导出后视频列表图标不刷新 —— ✅ 已修复

**现象**：从收藏夹进入细选，导出截图后，视频列表状态图标仍为⭐，未变为✅。

**根本原因**：`zoom_dialog.py` 的 `export_selected` 方法导出成功后未调用 `_notify_data_changed()` 触发主界面刷新。

**修复方案**：在 `export_selected` 中导出成功后添加 `self.controller._notify_data_changed()`。


## ✅ v2.5.0 已修复问题（历史记录）

1. **查看收藏窗口 - 初始显示窄线**（问题5a）
   - 改用 QGridLayout，延迟刷新，固定容器高度

2. **查看收藏窗口 - 最大化右侧空白**（问题5b）
   - 动态列数计算，图片尺寸自适应

3. **查看收藏窗口 - 绿点显示错误**（问题5c）
   - `_fix_old_exported()` 修复旧数据

4. **查看收藏窗口 - 布局左对齐问题**
   - 强制 AlignLeft，列拉伸为 0

5. **收藏窗口细选 - 找不到对应截图位置**
   - pos_in_segment 找不到时默认 0

6. **细选窗口 - 帧间隔不符合设计**
   - 固定偏移量，L1~L4 按设计间隔

7. **查看收藏窗口 - 多选细选无提示**
   - `_refresh_favorites` 保留选中状态

8. **查看收藏窗口 - 导出目录记忆失效**
   - 独立键 last_image_export_dir

9. **查看收藏窗口 - 移除替换原图按钮**
   - 已移除

10. **细选窗口 - 多选替换原图无提示**
    - 按钮始终可用，方法内检查

11. **细选窗口 - 第一次进入异步冲突**
    - QTimer.singleShot 延迟加载

12. **自动备份删除所有非今日的 .db 文件**
    - 至少保留一个最新备份


## ✅ 已完成优化项

| 编号 | 优化项                             | 状态     | 说明                                                  |
| ---- | ---------------------------------- | -------- | ----------------------------------------------------- |
| 1    | 导出图片默认位置记忆               | ✅ 已完成 | config_manager.py 增加 last_export_dir，图片/视频共用 |
| 2    | 动态分区范围 3~7 → 1~5             | ✅ 已完成 | 下拉框改为 1~5，默认 3，边界校验同步修改              |
| 3    | 时间戳样式黑底白字矩形块           | ✅ 已完成 | image_labels.py 绘制黑底白字，格式 h:mm:ss            |
| 4    | 排除区间改为视频级别（全局）       | ✅ 已完成 | 从 segments 表迁移到 videos 表，所有分区共用          |
| 5    | 主界面"查看收藏"按钮改名"收藏夹"   | ✅ 已完成 | 更符合直觉                                            |
| 6    | 细选窗口导出后自动刷新视频列表图标 | ✅ 已完成 | 导出后视频列表图标从⭐变为✅                            |
| 7    | 主界面显示版本号                   | ✅ 已完成 | src/version.py 定义 VERSION，窗口标题显示             |
| 8    | 信息区5行显示 + 分辨率自动获取     | ✅ 已完成 | 文件名/时长/分辨率/大小/路径，Arial 12 Bold，间距6px  |
| 9    | 放大预览自适应缩放 + 键盘方向键    | ✅ 已完成 | 首次打开适应窗口，←/→/↑/↓ 控制滚动条平移              |
| 10   | 收藏夹显示修复                     | ✅ 已完成 | 最大化按钮恢复，图片缩放适应                          |
| 11   | 代码优化（print→logger.debug 等）  | ✅ 已完成 | 边界检查、动态高度、退出报错修复                      |


## 🟡 待优化清单

| 编号 | 优化项 | 优先级 | 说明 | 计划版本 |
|------|--------|--------|------|----------|
| O1 | `QFlowLayout` 自定义布局移入独立文件 | 低 | 当前在 `segment_view.py` 中内联定义，建议移入 `ui/widgets/flow_layout.py` | v3.2 |
| O2 | `FavImageLabel` 继承优化 | 低 | 当前 `set_exported` 方法重复定义，可通过父类参数控制背景/缩放 | v3.2 |
| O3 | RuntimeError 深入排查 | 低 | 已暂搁置，不影响功能 | 待定 |


## 📦 自动备份与恢复功能说明

### 备份内容

自动备份功能保存的当前状态包括以下所有数据（完整数据库副本）：

1. **视频列表**：所有已导入视频的文件路径、文件名、时长、分辨率、文件大小、修改时间
2. **视频状态**：`is_viewed`（已浏览）、`is_starred`（有收藏）、`is_exported`（已导出）
3. **分区信息**：每个视频的 A/B/C 等分区的起止时间、`is_viewed`（该分区已加载）、`has_starred`（分区内有收藏）、`has_exported`（分区内有导出）
4. **收藏记录**：收藏截图所属视频、分区标签、时间戳（毫秒）、截图路径（NAS路径）、是否已导出
5. **排除区间**：每个视频的全局排除区间（视频级别，所有分区共用）
6. **数据库版本**：数据库架构版本号

> **注意**：备份文件为完整的 SQLite 数据库文件（`.db`），不包含截图图片文件（.jpg）和缓存文件。


### 自动恢复备份功能（✅ 已开发完成）

每次启动程序时，系统自动执行以下流程：

1. 从配置中读取备份目录路径
2. 扫描备份目录，查找所有 `coverpicker_backup_*.db` 文件
3. 按修改时间排序，找到最新的备份文件
4. 获取当前数据库文件（`~/.coverpicker/coverpicker.db`）的修改时间
5. 如果最新备份文件的修改时间 > 当前数据库的修改时间，则自动恢复
6. 恢复后**无需重启**，界面直接使用恢复后的数据库
7. 如果恢复失败，静默跳过，用户无感知


### 与"数据库备份自动恢复"（v3.2 待开发）的区别

| 对比项       | **自动恢复备份**（已开发）                       | **数据库备份自动恢复**（v3.2 待开发）     |
| ------------ | ------------------------------------------------ | ----------------------------------------- |
| **触发时机** | 每次软件启动时                                   | 启动时检测到数据库损坏后                  |
| **触发条件** | 备份文件比当前数据库更新                         | `PRAGMA quick_check` 检测到数据库损坏 |
| **恢复依据** | 文件修改时间                                     | 数据库完整性检查失败                      |
| **用户感知** | 静默执行，用户无感知                             | 恢复完成后提示用户，失败时显示错误        |
| **目的**     | 将数据库恢复到最新备份状态（用户主动备份的版本） | 在数据库损坏时自动修复，防止数据丢失      |


## 📊 整体完成度评估

| 功能模块                      | 权重 | 完成度     | 说明                                                                                                                               |
| ----------------------------- | ---- | ---------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| 视频分区浏览（7.2）           | 8%   | ✅ 100%     | 自动分段、切换正常；动态分区（1~5）已实现；自定义分区已实现                                                                        |
| Grid 候选浏览（7.3）          | 10%  | ✅ 100%     | 密度切换、截图显示正常；截图编号已实现；时间戳黑底白字矩形块                                                                       |
| 收藏管理（7.6）               | 10%  | ✅ 100%     | 收藏池、收藏夹弹窗正常；按分区分组显示；取消收藏删除NAS文件                                                                        |
| 锁定功能（7.3/7.4）           | 8%   | ✅ 100%     | 锁定/解锁、刷新未锁定、全部重抽正常                                                                                                |
| 导出功能（7.4/7.5）           | 10%  | ✅ 100%     | 单张导出正常；批量导出收藏截图已实现；视频片段导出已实现；路径记忆已实现                                                           |
| Zoom 细选（7.3/7.4）          | 15%  | ✅ 100%     | L1~L4 四层细选完整实现；单击选中、双击预览正常；帧间隔符合设计                                                                     |
| 左侧视频列表状态标识（7.1）   | 5%   | ✅ 100%     | ⭐/✅/👁️ 状态标识已实现，优先级 ✅ > ⭐ > 👁️ 已修正                                                                                      |
| 预览窗口（7.4）               | 12%  | ✅ 100%     | 独立对话框，视频帧预览 + 时间轴选段 + 片段导出，UI 优化                                                                            |
| 搜索/导入/批量删除（7.1）     | 5%   | ✅ 100%     | 搜索、导入、批量删除均已实现                                                                                                       |
| 清理缓存（7.5）               | 5%   | ✅ 100%     | 手动清理（清理所有历史缓存）                                                                                                       |
| 撤销/重做（7.4）              | 5%   | ✅ 100%     | 针对收藏/锁定操作                                                                                                                  |
| 排除区间（7.3）               | 5%   | ✅ 100%     | 已重构为视频级别（全局），持久化及跨设备同步已实现，支持编辑已有区间                                                               |
| 主题跟随（6.1）               | 5%   | ✅ 100%     | 跟随 Windows 系统深色/浅色模式                                                                                                     |
| 更多格式支持（附录）          | 5%   | ✅ 100%     | 扩展支持 20+ 种视频格式                                                                                                            |
| 独立可执行文件（附录）        | 5%   | ✅ 100%     | PyInstaller 目录模式打包                                                                                                           |
| NAS 级持久化（9）             | 12%  | ✅ 100%     | SQLite 持久化；启动时加载视频列表                                                                                                  |
| NAS 性能优化（9）             | 5%   | ✅ 100%     | 元数据缓存 + 并发控制 + 快速关键帧提取 + 缓存复用优化；取消任务即时响应 ✅ 已完成                                                  |
| 取消任务即时响应              | 新增 | ✅ 100%     | 切换分区/视频时立即终止 FFmpeg 进程                                                                                                |
| 跨设备状态同步                | 新增 | ✅ 100%     | 使用 file_id（文件名+大小+修改时间哈希）跨设备识别同一视频                                                                         |
| NAS收藏持久化                 | 新增 | ✅ 100%     | 收藏截图永久保存在 [视频目录]/[视频名]_covers/；保存失败时向用户提示                                                               |
| 自动备份                      | 新增 | ✅ 100%     | 关闭窗口时自动备份当前状态到指定目录；至少保留一个最新备份                                                                         |
| 自动恢复备份                  | 新增 | ✅ 100%     | 每次启动时自动用更新的备份覆盖当前数据库                                                                                           |
| 智能监控（增量扫描+自动发现） | 新增 | ✅ 100%     | 文件监控 + 60秒定时扫描 + 删除检测；跳过_covers目录                                                                                |
| 崩溃报告与日志增强            | 新增 | ✅ 100%     | 全局异常捕获 + 崩溃报告生成 + 日志文件轮转                                                                                         |
| 截图提取失败重试              | 新增 | ✅ 100%     | 自动重试最多3次，减少占位图                                                                                                        |
| 缓存优化（固定目录）          | 新增 | ✅ 100%     | 缓存目录固定，退出不删除，统计和清理功能正常                                                                                       |
| 主界面显示版本号（v3.0.1）    | 新增 | ✅ 100%     | src/version.py 定义 VERSION，窗口标题显示                                                                                          |
| 信息区5行显示（v3.0.1）       | 新增 | ✅ 100%     | 文件名/时长/分辨率/大小/路径，Arial 12 Bold，间距6px，顶部对齐                                                                     |
| 分辨率自动获取（v3.0.1）      | 新增 | ✅ 100%     | 导入时用 ffprobe 获取分辨率，存入数据库                                                                                            |
| 放大预览自适应缩放（v3.0.1）  | 新增 | ✅ 100%     | 首次打开适应窗口，滚轮缩放 + 键盘方向键平移                                                                                        |
| 收藏夹显示修复（v3.0.1）      | 新增 | ✅ 100%     | 最大化按钮恢复，图片缩放适应                                                                                                      |

**加权综合完成度：约 98%**


## 🛠️ 技术栈

| 组件     | 版本               |
| -------- | ------------------ |
| Python   | 3.13               |
| GUI 框架 | PySide6 6.11.1     |
| 异步框架 | asyncio + qasync   |
| 视频处理 | FFmpeg / FFprobe   |
| 数据库   | SQLite             |
| 打包工具 | PyInstaller 6.21.0 |


## 📁 当前项目结构
CoverPicker/
├── main.py # 主入口（含崩溃报告 + 日志）
├── src/
│ ├── version.py # 版本号定义（v3.0.1 新增）
│ ├── config_manager.py # 配置管理
│ ├── database.py # SQLite 数据库操作
│ ├── video_scanner.py # 视频扫描、FFmpeg 提取（v3.0.1 新增 get_video_resolution）
│ ├── logger_setup.py # 日志配置
│ ├── crash_handler.py # 全局异常捕获 + 崩溃报告
│ └── controllers/
│ └── segment_controller.py # 业务逻辑控制器（v3.0.1 新增分辨率支持）
├── ui/
│ ├── views/
│ │ ├── segment_view.py # 主界面（v3.0.1 信息区5行）
│ │ ├── zoom_dialog.py # Zoom 细选对话框
│ │ ├── zoom_preview.py # 图片预览对话框（v3.0.1 自适应缩放 + 键盘方向键）
│ │ ├── preview_dialog.py # 视频预览窗口
│ │ └── exclude_dialog.py # 排除区间设置
│ ├── dialogs/
│ │ └── favorites_dialog.py # 收藏夹弹窗（v3.0.1 显示修复）
│ └── widgets/
│ └── image_labels.py # ClickableLabel + FavImageLabel
├── log/ # 日志目录
│ ├── CoverPicker.log # 主日志文件（轮转）
│ └── crashes/ # 崩溃报告目录
└── PROGRESS.md # 项目进度日志


## 📝 开发日志

### 2026-07-26 (v3.0.1 状态确认 + RuntimeError 排查)

**确认事项**：
1. v2.5.2 完全恢复后，快速切换视频**无 RuntimeError** ✅
2. v3.0.1 功能已全部实现（版本号、信息区5行、分辨率、放大预览、收藏夹修复）✅
3. main 分支快速切换视频有 `RuntimeError`，不影响功能 ⚠️
4. 经过逐文件排查，单个改动均不是原因，可能为组合效应 ⚠️

**排查过程**：
- 恢复了 `favorites_dialog.py`、`main.py`、`image_labels.py` → 报错仍在
- 恢复 `segment_controller.py` + `segment_view.py` 一起 → 报错消失
- 细化排查 `segment_view.py` 信息区改动（QTextEdit→QLabel、addStretch、分辨率行、拉伸因子）→ 均不是原因
- 恢复 `segment_controller.py` 到 v2.5.2 → 累积性报错
- 清空缓存测试 → 报错仍在

**结论**：
- RuntimeError 不影响程序功能，决定暂时搁置
- 继续开发 v3.2 功能


### 2026-07-25 (v3.0.1 发布)

**完成功能**：

1. **主界面显示版本号**
   - 新增 `src/version.py`，定义 `VERSION = "3.0.1"`
   - 主窗口标题显示 `CoverPicker - v3.0.1`
   - 启动日志显示版本号

2. **左侧信息区优化为 5 行**
   - 显示：文件名 / 时长 / 分辨率 / 文件大小 / 路径
   - 全部字体统一为 Arial 12 Bold
   - 行间距 6px，顶部对齐，底部 stretch 吸收多余空间
   - 视频列表拉伸因子 2→3

3. **分辨率自动获取**
   - 新增 `get_video_resolution()` 函数
   - 导入时自动获取并存入数据库

4. **放大预览窗口自适应缩放**
   - 在 `showEvent` 中延迟 50ms 计算初始缩放
   - `fit_scale = min(视口宽/图片宽, 视口高/图片高)`

5. **放大预览窗口键盘方向键平移**
   - `←/→` 水平 ±20px，`↑/↓` 垂直 ±20px
   - ESC 关闭窗口

6. **收藏夹显示修复**
   - 恢复最大化/最小化按钮
   - 图片缩放适应显示

7. **代码优化**
   - `print` → `logger.debug()`
   - 边界检查
   - 退出报错修复


### 2026-07-24 (v2.5.2)

**修复：问题4（长视频路径显示不完整）**

- **现象**：特定长文件名视频路径在左侧信息区显示不完整
- **根本原因**：`QLabel` 的 `wordWrap` 在包含反斜杠的长字符串中无法正确计算高度；信息组无垂直扩展能力
- **修复方案**：
  1. 将 `info_path` 从 `QLabel` 替换为 `QTextEdit`（只读、无边框、透明背景）
  2. 信息组拉伸因子从0改为1
  3. `QTextEdit` 使用完整文档引擎，能正确计算并显示所有行

**修复：问题8（视频状态图标优先级错误）**

- **根本原因**：`get_video_state_icon` 优先级为 ⭐ > ✅ > 👁️
- **修复方案**：改为 ✅ > ⭐ > 👁️（符合 PRODUCT.md 7.1）

**修复：问题9（NAS收藏静默回退）**

- **根本原因**：`_save_favorite_to_nas` 失败时静默返回临时路径
- **修复方案**：返回值改为 `Tuple[str, bool]`，通过 `_notify_progress` 向用户提示

**修复：细选窗口导出后视频列表图标不刷新**

- **根本原因**：`zoom_dialog.py` 的 `export_selected` 未触发主界面刷新
- **修复方案**：添加 `self.controller._notify_data_changed()`


### 2026-07-23 (v2.5.1)

**修复：排除区间架构重构（问题7：C区截图不显示）**

- **现象**：特定视频点击C区后跳回A区显示空白
- **根本原因**：排除区间被错误设计为分区级别
- **修复方案**：
  1. 排除区间从 `segments` 表迁移到 `videos` 表，改为视频级别（全局）
  2. `load_excluded_ranges_from_db` 从 `videos` 表读取，不再重置 `current_seg_index`


### 2026-07-23 (v2.5.0)

**查看收藏窗口与细选功能修复版**

修复的问题：
- 查看收藏窗口窄线、右侧空白、绿点错误、布局左对齐
- 收藏窗口细选位置找不到
- 细选窗口帧间隔不符合设计
- 查看收藏窗口多选细选无提示
- 查看收藏窗口导出目录记忆失效
- 查看收藏窗口移除替换原图按钮
- 细选窗口多选替换原图无提示
- 细选窗口第一次进入异步冲突
- 自动备份删除所有 .db 文件


## 🚧 后续计划

### 版本路线

| 版本   | 核心目标                                                              | 状态     |
| ------ | --------------------------------------------------------------------- | -------- |
| v2.5.0 | 查看收藏窗口与细选功能修复                                            | ✅ 已发布 |
| v2.5.1 | 排除区间架构重构 + 隐患识别                                           | ✅ 已发布 |
| v2.5.2 | 路径显示修复 + 状态图标优先级 + NAS收藏提示 + 细选导出刷新            | ✅ 已发布（稳定基准版本） |
| v3.0.1 | 主界面显示版本号 + 信息区5行 + 分辨率自动获取 + 放大预览自适应缩放    | ✅ 已发布（已知 RuntimeError 已记录） |
| v3.2   | 数据库备份自动恢复 + GIF动态预览导出 + 截图快速切换 + 导出命名优化 + 打开导出夹按钮 | ⏳ 待开发 |
| v3.3   | AI 偏好学习与智能推荐（CLIP + Chroma）                                | ⏳ 待开发 |


### v3.2 详细计划

| 编号 | 功能 | 预计工时 | 验收标准 |
|------|------|----------|----------|
| 3.2.1 | 数据库备份自动恢复 | 4-6h | `PRAGMA quick_check`，损坏时自动恢复，成功率≥95% |
| 3.2.2 | GIF 动态预览导出 | 5-7h | 导出对话框含帧率/尺寸/循环选项，最长30秒，自动裁剪提示 |
| 3.2.3 | 截图快速切换 | 2-3h | 任何界面双击放大预览窗口支持键盘左右键切换 |
| 3.2.4 | 导出命名优化 | 1h | `[视频名]_cover_[时间].jpg`，同时记录 `video_id` |
| 3.2.5 | 打开导出夹按钮 | 1h | 所有有导出按钮的界面增加该按钮 |


### v3.3 详细计划

| 阶段 | 内容 | 预计工时 |
|------|------|----------|
| 预览版 | CLIP模型加载 + 特征提取 + SQLite存储 + 分类学习 + UI + 测试 | 9-13h |
| 正式版 | 后台自动学习 + 推荐排序优化 + 全局开关 + 用户引导 | 6-8h |


## 📌 备注

- 所有修改遵循"稳定性 > 性能 > 新增功能"原则。
- 用户操作状态长期本地持久化，重启可续做。
- 仅保留手动"清理缓存"，无自动清理机制。
- 软件绝不删除任何视频文件，仅删除截图文件（.jpg）。
- video_scanner.py 中的 FFmpeg 参数已稳定运行，如需调整必须先向用户索取原始文件，不得擅自重写。
- v2.5.2 是稳定的基准版本，无 RuntimeError。
- v3.0.1 功能已全部实现，RuntimeError 已记录为已知问题，不影响功能，暂搁置。
- 自动备份功能保存完整数据库状态（6类数据），自动恢复备份功能已开发完成。
- **核心布局方案已冻结**（见 PRODUCT.md 附录D），后续开发不得破坏。

**最后更新：2026-07-26**
**状态：✅ v3.0.1 功能已实现，RuntimeError 已记录（不影响功能）**
**稳定基准版本：v2.5.2**
**对应 PRODUCT.md 版本：v6.4**