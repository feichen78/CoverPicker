📋 CoverPicker 项目进度日志
🏷️ 当前版本信息
版本号： v2.1.4

版本名称： 收藏弹窗全面修复版

发布日期： 2026-07-19

状态： ✅ 稳定运行

对应 PRODUCT.md 版本： v4.3

📌 开发协作规则（2026-07-19 更新）
代码提供方式
任何时候都必须以精简格式（删除多余空行和注释）提供完整文件代码，并在开头注明文件路径，不允许只提供部分代码。
提供完整代码前，不需要列出修改内容1…… 修改内容2……。

改错规则
明确的报错代码（Traceback）：先分析原因，确认后提供完整代码
无明确报错代码（仅文字描述现象）：先与用户确认现象，再分析原因，有必要的话要增加输出日志，确保无误后提供修改好的完整代码。提供修复代码前不需要用户确认原因分析是否正确。

修改规则
修改 bug 或增加功能时，如果需要删减已有的功能，必须向用户确认。没有确认则不允许删除任何已有功能。

📊 整体完成度评估
功能模块	权重	完成度	说明
视频分区浏览（7.2）	8%	100%	自动分段、切换正常；动态分区（3~7）已实现；自定义分区已实现
Grid 候选浏览（7.3）	10%	100%	密度切换、截图显示正常；截图编号已实现；时间戳格式 hh:mm:ss 亮橙色
收藏管理（7.6）	10%	100%	收藏池、查看收藏弹窗正常；✅ 选中计数、点击定位、细选、替换原图全部修复
锁定功能（7.3/7.4）	8%	100%	锁定/解锁、刷新未锁定、全部重抽正常
导出功能（7.4/7.5）	10%	100%	单张导出正常；批量导出收藏截图已实现；视频片段导出已实现
Zoom 细选（7.3/7.4）	15%	100%	L1~L4 四层细选完整实现；✅ 收藏弹窗替换原图功能已修复
左侧视频列表状态标识（7.1）	5%	100%	⭐/✅/👁️ 状态标识已实现，优先级：收藏 > 已导出 > 已查看
预览窗口（7.4）	12%	100%	独立对话框，视频帧预览 + 时间轴选段 + 片段导出
搜索/导入/批量删除（7.1）	5%	100%	搜索、导入、批量删除均已实现
清理缓存（7.5）	5%	100%	手动清理 + 自动清理（超过 5GB 阈值）
撤销/重做（7.4）	5%	100%	针对收藏/锁定操作
排除区间（7.3）	5%	100%	已恢复，持久化及跨设备同步已实现，时间格式 hh:mm:ss
主题跟随（6.1）	5%	100%	跟随 Windows 系统深色/浅色模式
更多格式支持（附录）	5%	100%	扩展支持 20+ 种视频格式
独立可执行文件（附录）	5%	100%	PyInstaller 目录模式打包
NAS 级持久化（9）	12%	100%	SQLite 持久化；启动时加载视频列表
NAS 性能优化（9）	5%	100%	元数据缓存 + 并发控制 + 快速关键帧提取
跨设备状态同步	新增	100%	使用 file_id（文件名+大小+修改时间哈希）跨设备识别同一视频
加权综合完成度：约 100%

✅ 已完成功能
基础框架与界面
PySide6 桌面应用

综合布局：左侧视频列表 + 右侧主工作区

启动时从数据库加载视频列表（移除硬编码扫描）

视频信息显示：时长、大小

动态分区：3~7 分区自由切换（下拉框选择）

自定义分区：预览窗口中通过分割点自定义分区

网格密度切换：9/12/16/25 张布局

排除区间：手动设置排除时间段，格式 hh:mm:ss

批量删除：Ctrl+点击 / Shift+点击 多选后批量删除（不删除文件）

左侧视频列表状态标识（👁️/⭐/✅）优先级：⭐ > ✅ > 👁️

搜索视频：实时过滤视频列表

导入视频：支持文件 + 文件夹导入，基于文件名去重

清理缓存：手动清理 + 自动清理（超过 5GB 提示）

撤销/重做：针对收藏/锁定操作

主题跟随：跟随 Windows 系统深色/浅色模式

独立可执行文件：PyInstaller 目录模式打包

截图生成与显示
异步 FFmpeg 提取关键帧（隐藏 cmd 窗口）

快速关键帧提取（-skip_frame nokey）

截图自动裁剪片头片尾

截图网格自适应显示（保持宽高比）

每张截图左下角显示时间戳（hh:mm:ss 格式，亮橙色）

左上角显示截图序号

选中状态：左上角蓝色圆点（全选按钮切换模式）

锁定状态：🔒 标记

缓存复用优化（放宽匹配条件到 1.0 秒）

收藏管理（7.6）
收藏/取消收藏选中截图（⭐ 标记）

左侧面板显示收藏总数

查看收藏弹窗（自适应布局，支持最大化）

收藏弹窗中正确显示 ⭐ 和绿色导出圆点

收藏弹窗支持 Zoom 细选入口（细选按钮始终可点击，由逻辑控制提示）

收藏弹窗中选中计数准确 ✅ v2.1.4

收藏弹窗中点击定位准确 ✅ v2.1.4

收藏弹窗中时间戳显示与主界面一致（左下角亮橙色 hh:mm:ss）✅ v2.1.4

收藏弹窗中替换原图功能正常 ✅ v2.1.4

收藏弹窗中多选点击细选有提示 ✅ v2.1.4

SQLite 持久化：收藏记录、导出状态完整保存和恢复

排除区间状态持久化及跨设备同步

跨设备状态同步
使用 file_id（文件名+大小+修改时间哈希）跨设备识别同一视频

本地访问 NAS 与 WebDAV 远程访问 NAS 状态保持一致

备份/恢复机制支持跨设备状态同步

锁定功能
锁定/解锁选中截图

刷新未锁定

全部重抽

导出功能
导出截图到自选目录

导出后截图右下角绿点标记（收藏弹窗内正常）

批量导出收藏截图到自选目录

视频片段导出到自选目录

支持 WMV、FLV、WEBM、AVI、MOV、MKV 等格式自动重编码

Zoom 细选（L1~L4）
L1（初选）：±4 秒范围，9 张候选帧

L2（精选）：±2 秒范围，9 张候选帧

L3（细选）：±1 秒范围，9 张候选帧

L4（定稿）：±0.5 秒范围，9 张候选帧

触发入口：主界面 + 收藏弹窗

交互规则：单击选中 · 双击放大预览

全选 / 取消全选 切换模式

支持多选收藏

支持多选导出

替换原图功能：主界面 + 收藏弹窗均正常 ✅ v2.1.4

预览窗口（独立对话框）
独立可调整大小窗口

视频帧预览（FFmpeg 提取）

时间轴滑块浏览

时间轴刻度：显示 0%、25%、50%、75%、100% 位置时间标签

片段范围选择（设起始/设结束）

视频片段导出到自选目录

自定义分区（分割点模式）

交互操作
单击：选中/取消选中

双击：放大预览（滚轮缩放 + 拖动平移）

视频列表：双击加载视频，单击仅选中（不加载）

全选 / 取消全选（切换模式）

Ctrl+点击 / Shift+点击 多选

状态持久化
_save_state_to_db() 正确计算 is_starred、is_exported、is_viewed

收藏表增量更新

导出状态同步到 favorites.is_exported

分区按钮显示时间范围

视频列表状态图标启动刷新

启动时从数据库加载视频列表

元数据缓存：文件未变化时跳过 FFprobe 调用

排除区间持久化到数据库

跨设备 file_id 识别

协程任务管理
load_video() 取消之前的加载任务

_load_segment() 正确处理 CancelledError

refresh_unlocked() 和 reset_all() 捕获取消异常

closeEvent() 清理任务

并发控制
信号量（Semaphore）限制 FFmpeg 进程数（默认 3 个）

代码架构
控制器分离：segment_controller.py 管理业务逻辑

预览控制器分离：preview_controller.py

自定义控件分离：image_labels.py

对话框分离：favorites_dialog.py, exclude_dialog.py

预览窗口独立：preview_dialog.py

打包构建
PyInstaller 目录模式打包

FFmpeg 调用隐藏 cmd 窗口

导出目录自定义选择

UPX 压缩支持

🚧 未完成功能（待后续优化）
1. 渐进式加载（占位图）- 提升用户体验
2. 增量扫描与自动发现（文件监控）- 自动检测新视频（已完成基础框架）
3. 智能分区（内容感知）- 基于场景切换自动分区（需 FFmpeg scene 检测）
4. 命令行接口（自动化）- 支持脚本调用
5. 崩溃报告与日志增强 - 自动收集错误
6. macOS 10.15.8 版本打包

✅ 已修复问题（v2.1.4）
问题	状态	说明
排除区间时间格式	✅ 已修复	从秒数改为 hh:mm:ss
排除区间跨设备同步	✅ 已修复	通过 SQLite 持久化 + 云同步目录实现
视频状态跨设备同步	✅ 已修复	使用 file_id 识别同一视频
导入文件夹不递归	✅ 已修复	使用 scan_videos 替代 scan_videos_in_directory
分区切换无响应	✅ 已修复	直接调用 _load_segment 绕过 load_segment 检查
查看收藏窗口细选按钮缺失	✅ 已恢复	在 favorites_dialog 中添加细选按钮
收藏弹窗选中计数错误	✅ 已修复	使用 selected_keys (segment, time) 替代索引
收藏弹窗点击定位错位	✅ 已修复	通过标签索引定位，避免手动计算偏移
收藏弹窗时间戳格式	✅ 已修复	改为左下角亮橙色 hh:mm:ss
收藏弹窗替换原图失败	✅ 已修复	新增 replace_favorite_screenshot 方法
收藏弹窗多选细选无反应	✅ 已修复	细选按钮始终可用，由逻辑控制提示
视频列表状态图标优先级	✅ 已修复	⭐ > ✅ > 👁️

📁 当前项目结构
CoverPicker/
├── main.py                          # 主入口
├── src/
│   ├── config_manager.py            # 配置管理
│   ├── database.py                  # SQLite 数据库操作（含 file_id）
│   ├── video_scanner.py             # 视频扫描、FFmpeg 提取
│   └── controllers/
│       └── segment_controller.py    # 业务逻辑控制器
├── ui/
│   ├── views/
│   │   ├── segment_view.py          # 主界面
│   │   ├── zoom_dialog.py           # Zoom 细选对话框
│   │   ├── zoom_preview.py          # 图片预览对话框
│   │   ├── preview_dialog.py        # 视频预览窗口
│   │   └── exclude_dialog.py        # 排除区间设置
│   ├── dialogs/
│   │   └── favorites_dialog.py      # 收藏弹窗 ✅ v2.1.4 全面修复
│   └── widgets/
│       └── image_labels.py          # 自定义图片标签
└── PROGRESS.md                      # 项目进度日志

🛠️ 技术栈
组件	版本
Python	3.13
GUI 框架	PySide6 6.11.1
视频处理	FFmpeg / FFprobe
异步框架	asyncio + qasync
数据库	SQLite
打包工具	PyInstaller 6.21.0

📝 开发日志（2026-07-19）
v2.1.4 修复
收藏弹窗全面修复：

选中计数：从 selected_indices 改为 selected_keys，使用 (segment, time) 作为唯一标识

点击定位：通过标签索引定位，避免手动计算偏移导致错位

时间戳格式：统一为左下角亮橙色 hh:mm:ss，移除多余时间标签

替换原图：新增 replace_favorite_screenshot 方法，独立处理收藏截图替换

多选细选：细选按钮始终可用，由 zoom_selected 方法检查选中数量并提示

视频列表状态图标优先级：调整为 ⭐ > ✅ > 👁️

📌 后续计划
1. 渐进式加载（占位图）
2. macOS 10.15.8 版本打包


最后更新： 2026-07-19