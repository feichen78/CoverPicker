📋 CoverPicker 项目进度日志
🏷️ 当前版本信息
项目	内容
版本号	v2.4.1
版本名称	稳定性修复与UI优化版
发布日期	2026-07-21
状态	✅ 稳定运行
对应 PRODUCT.md 版本	v5.1
📊 整体完成度评估
功能模块	权重	完成度	说明
视频分区浏览（7.2）	8%	✅ 100%	自动分段、切换正常；动态分区（3~7）已实现；自定义分区已实现
Grid 候选浏览（7.3）	10%	✅ 100%	密度切换、截图显示正常；截图编号已实现；时间戳 hh:mm:ss 亮橙色
收藏管理（7.6）	10%	✅ 100%	收藏池、查看收藏弹窗正常；按分区分组显示；取消收藏删除NAS文件
锁定功能（7.3/7.4）	8%	✅ 100%	锁定/解锁、刷新未锁定、全部重抽正常
导出功能（7.4/7.5）	10%	✅ 100%	单张导出正常；批量导出收藏截图已实现；视频片段导出已实现
Zoom 细选（7.3/7.4）	15%	✅ 100%	L1~L4 四层细选完整实现；单击选中、双击预览正常
左侧视频列表状态标识（7.1）	5%	✅ 100%	⭐/✅/👁️ 状态标识已实现，优先级正确
预览窗口（7.4）	12%	✅ 100%	独立对话框，视频帧预览 + 时间轴选段 + 片段导出，UI 优化
搜索/导入/批量删除（7.1）	5%	✅ 100%	搜索、导入、批量删除均已实现
清理缓存（7.5）	5%	✅ 100%	手动清理（清理所有历史缓存）+ 自动清理（超过 5GB 阈值）
撤销/重做（7.4）	5%	✅ 100%	针对收藏/锁定操作
排除区间（7.3）	5%	✅ 100%	已恢复，持久化及跨设备同步已实现，支持编辑已有区间
主题跟随（6.1）	5%	✅ 100%	跟随 Windows 系统深色/浅色模式
更多格式支持（附录）	5%	✅ 100%	扩展支持 20+ 种视频格式
独立可执行文件（附录）	5%	✅ 100%	PyInstaller 目录模式打包
NAS 级持久化（9）	12%	✅ 100%	SQLite 持久化；启动时加载视频列表
NAS 性能优化（9）	5%	✅ 100%	元数据缓存 + 并发控制 + 快速关键帧提取 + 进程池
跨设备状态同步	新增	✅ 100%	使用 file_id（文件名+大小+修改时间哈希）跨设备识别同一视频
NAS收藏持久化	新增	✅ 100%	收藏截图永久保存在 [视频目录]/[视频名]_covers/
自动备份	新增	✅ 100%	关闭窗口时自动备份当前状态到指定目录；启动时删除旧备份
智能监控（增量扫描+自动发现）	新增	✅ 100%	文件监控 + 60秒定时扫描 + 删除检测；跳过_covers目录
崩溃报告与日志增强	新增	✅ 100%	全局异常捕获 + 崩溃报告生成 + 日志文件轮转
截图提取失败重试	新增	✅ 100%	自动重试最多3次，减少占位图
缓存优化（固定目录）	新增	✅ 100%	缓存目录固定，退出不删除，统计和清理功能正常
网格延迟加载	新增	✅ 100%	采用 QListView + Model/Delegate 实现，性能良好
任务并发与取消	新增	✅ 100%	使用 asyncio + 信号量实现并发提取，取消即时响应
查看收藏窗口	新增	✅ 100%	按分区分组显示，自适应列数，充分利用空间
加权综合完成度		约 100%	所有核心功能稳定，体验优化完成
✅ 已完成功能（v2.4.1）
核心交互
主界面布局（左-中-右），视频列表、分区按钮、截图网格、底部操作栏。

网格延迟加载（QListView + 自定义委托），大幅降低内存占用。

单击选中/取消选中，双击放大预览（滚轮缩放 + 拖动平移）。

全选/取消全选、键盘快捷键（Ctrl+A, Ctrl+D, Delete, Space, 方向键）。

分区切换、密度切换（9/12/16/25）。

排除区间设置（支持编辑已有区间、持久化、跨设备同步）。

收藏、锁定、导出、刷新未锁定、全部重抽。

后台与性能
异步 FFmpeg 帧提取（asyncio + 信号量控制并发）。

缓存复用（1 秒内时间差匹配）。

固定缓存目录（%TEMP%/CoverPicker_cache），退出不删除。

缓存统计（大小、文件数）及手动清理。

任务取消即时响应（切换分区时 kill 子进程）。

视频加载时界面立即更新（显示占位图、视频名称提前显示）。

数据持久化
SQLite 数据库存储视频、分区、收藏、排除区间。

跨设备状态同步（通过 file_id 识别）。

自动备份（关闭窗口时备份至指定目录）。

崩溃报告生成（异常捕获 + 日志轮转）。

辅助功能
预览窗口（视频帧预览、时间轴选段、自定义分区、片段导出）。

智能监控（目录变化自动导入、定时扫描、删除检测；跳过_covers目录）。

导入视频（文件/文件夹，基于文件名去重）。

批量删除视频（仅删除数据库记录，不删除视频文件）。

收藏管理增强
查看收藏窗口按分区分组显示。

自适应列数（1~6列），充分利用窗口空间。

取消收藏时删除NAS上的图片文件。

窗口标题和数量实时更新。

🔧 已修复问题（v2.4.1）
问题	状态	说明
排除区间时间格式	✅ 已修复	从秒数改为 hh:mm:ss
排除区间跨设备同步	✅ 已修复	通过 SQLite 持久化 + 云同步目录实现
视频状态跨设备同步	✅ 已修复	使用 file_id 识别同一视频
导入文件夹不递归	✅ 已修复	使用 scan_videos 替代 scan_videos_in_directory
分区切换无响应	✅ 已修复	直接调用 _load_segment 绕过 load_segment 检查
查看收藏窗口细选按钮缺失	✅ 已恢复	在 favorites_dialog 中添加细选按钮
收藏弹窗选中计数错误	✅ 已修复	使用 selected_keys (segment, time) 替代索引
收藏弹窗点击定位错位	✅ 已修复	通过标签索引定位，避免手动计算偏移
收藏弹窗时间戳格式	✅ 已修复	改为左下角亮橙色 hh:mm:ss
收藏弹窗替换原图失败	✅ 已修复	新增 replace_favorite_screenshot 方法
收藏弹窗多选细选无反应	✅ 已修复	细选按钮始终可用，由逻辑控制提示
视频列表状态图标优先级	✅ 已修复	⭐ > ✅ > 👁️
跨设备收藏截图丢失	✅ 已修复	收藏截图永久保存到NAS，跨设备共享
关闭时手动备份繁琐	✅ 已修复	关闭窗口自动备份
自动导入不完整	✅ 已修复	去重改为完整路径，递归添加子目录监控
密度切换布局错乱	✅ 已修复	清空 screenshots 后重新加载，重置布局列/行拉伸
异步任务冲突导致布局错误	✅ 已修复	取消任务时清空 screenshots，等待取消完成后再创建新任务
选中计数不同步	✅ 已修复	增加日志定位，确保 selected_indices 与界面同步
排除区间覆盖整个分区时崩溃	✅ 已修复	添加死循环保护，检测全部排除时直接返回
预览窗口字体不一致	✅ 已修复	统一为 13pt Arial
缓存清理仅清理当前会话	✅ 已修复	改为清理所有历史 CoverPicker_* 临时目录
截图加载失败显示黑色占位图	✅ 已修复	增加重试机制（最多3次），减少失败率
排除区间自适应分区功能回退	✅ 已修复	恢复 set_num_segments 中的判断逻辑
ClickableLabel Expanding 属性错误	✅ 已修复	使用 QSizePolicy.Expanding 替代错误的调用
主界面取消收藏删除缓存文件	✅ 已修复	改为只删除NAS图片，保留缓存文件
主界面取消收藏NAS图片未删除	✅ 已修复	从 favorites 列表中获取NAS路径并删除
查看收藏窗口取消收藏NAS图片未删除	✅ 已修复	unfavorite_by_time 正确删除NAS文件
查看收藏窗口取消收藏数量不更新	✅ 已修复	_refresh_favorites 更新窗口标题
查看收藏窗口所有截图显示绿点	✅ 已修复	从数据库正确读取 exported 状态
查看收藏窗口图片混排	✅ 已修复	按分区分组显示，自适应列数
ZoomDialog 单击/双击无反应	✅ 已修复	on_label_click 和 _preview_single 增加 idx=None 参数
密度切换卡死	✅ 已修复	on_density_changed 取消旧任务
监控目录误触发	✅ 已修复	_setup_watch_dirs 跳过 _covers 目录
📁 当前项目结构
text
CoverPicker/
├── main.py                          # 主入口（含崩溃报告 + 日志）
├── log/                             # 日志目录
│   ├── CoverPicker.log              # 主日志文件（轮转）
│   └── crashes/                     # 崩溃报告目录
│       └── crash_report_*.txt       # 崩溃报告
├── src/
│   ├── config_manager.py            # 配置管理
│   ├── database.py                  # SQLite 数据库操作
│   ├── video_scanner.py             # 视频扫描、FFmpeg 提取（含异步版本）
│   ├── logger_setup.py              # 日志配置（文件+控制台+轮转）
│   ├── crash_handler.py             # 全局异常捕获 + 崩溃报告
│   └── controllers/
│       └── segment_controller.py    # 业务逻辑控制器（任务管理、缓存、收藏）
├── ui/
│   ├── views/
│   │   ├── segment_view.py          # 主界面（网格、列表、控制栏）
│   │   ├── zoom_dialog.py           # Zoom 细选对话框
│   │   ├── zoom_preview.py          # 图片预览对话框（居中滚动）
│   │   ├── preview_dialog.py        # 视频预览窗口
│   │   └── exclude_dialog.py        # 排除区间设置
│   ├── dialogs/
│   │   └── favorites_dialog.py      # 收藏弹窗（按分区分组，自适应列数）
│   └── widgets/
│       └── image_labels.py          # ClickableLabel + FavImageLabel
└── PROGRESS.md                      # 项目进度日志
🛠️ 技术栈
组件	版本
Python	3.13
GUI 框架	PySide6 6.11.1
异步框架	asyncio + qasync
视频处理	FFmpeg / FFprobe
数据库	SQLite
打包工具	PyInstaller 6.21.0
📝 开发日志（v2.4.1）
2026-07-21
修复 ZoomDialog 单击/双击无反应：on_label_click 和 _preview_single 增加 idx=None 参数。

修复查看收藏窗口图片混排：改为按分区分组显示，自适应列数（1~6列），充分利用窗口空间。

修复查看收藏窗口绿点显示错误：_restore_favorites_from_db 从数据库正确读取 exported 状态。

修复查看收藏窗口数量不更新：_refresh_favorites 更新窗口标题。

修复主界面取消收藏NAS图片未删除：从 favorites 列表中获取NAS路径并删除。

修复主界面取消收藏删除缓存文件：改为只删除NAS图片，保留缓存文件。

修复密度切换卡死：on_density_changed 取消旧任务。

修复监控目录误触发：_setup_watch_dirs 跳过 _covers 目录。

优化查看收藏窗口布局：每个分区独立显示，分区标题显示为 A区 (9 张)，空分区不显示。

恢复自动备份功能：关闭窗口自动备份；启动时删除旧备份文件。

🚧 后续计划
当前所有核心功能已稳定，后续版本将按 PRODUCT.md 附录 C 推进：

版本	核心目标	预计工时	状态
v3.0	性能优化（进程池+延迟加载+取消任务即时响应）	12-18h	✅ 已完成
v3.1	缓存升级（分级管理+预缓存+元数据预扫描+内存监控）	14-20h	⏳ 待开发
v3.2	数据库备份自动恢复 + GIF动态预览导出 + macOS打包	14-22h	⏳ 待开发
v3.3	AI 偏好学习与智能推荐（CLIP + Chroma）	10-16h	⏳ 待开发
📌 备注
所有修改遵循“稳定性 > 性能 > 新增功能”原则。

用户操作状态长期本地持久化，重启可续做。

缓存自动清理机制已就绪。

软件绝不删除任何视频文件，仅删除截图文件（.jpg）。

最后更新： 2026-07-21
状态： 所有核心功能稳定，体验优化完成，可发布 v2.4.1