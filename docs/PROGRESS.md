📋 CoverPicker 项目进度日志
🏷️ 当前版本信息
版本号： v1.2.0

版本名称： 清理缓存 + 撤销/重做 + 批量删除 + 排除区间恢复稳定版

发布日期： 2026-07-15

状态： ✅ 稳定运行

对应 PRODUCT.md 版本： v4.1

📊 整体完成度评估（对照 PRODUCT.md v4.1）
功能模块	权重	完成度	说明
视频分区浏览（7.2）	8%	95%	自动分段、切换正常；动态分区（3~7）已实现；自定义分区已实现；时间范围显示已实现
Grid 候选浏览（7.3）	10%	95%	密度切换、截图显示正常；截图编号已实现；时间戳格式 hh:mm:ss
收藏管理（7.6）	10%	95%	收藏池、查看收藏弹窗、导出状态同步；SQLite 持久化；FavImageLabel 已修复
锁定功能（7.3/7.4）	8%	95%	锁定/解锁、刷新未锁定、全部重抽正常
导出功能（7.4/7.5）	10%	80%	单张导出正常；批量导出收藏截图已实现；视频片段导出已实现
Zoom 精修（7.3/7.4）	15%	100%	L1~L4 四层精修完整实现（主界面 + 收藏弹窗）
左侧视频列表状态标识（7.1）	5%	95%	👁️/⭐/✅ 状态标识已实现，启动刷新已修复
预览窗口（7.4）	12%	100%	独立对话框，视频帧预览 + 时间轴选段 + 片段导出；自定义分区；时间轴刻度
搜索/导入/批量删除（7.1）	5%	90%	搜索、导入、批量删除（含 Shift 多选）均已实现
清理缓存（7.5）	5%	100%	已实现
撤销/重做（7.4）	5%	100%	针对收藏/锁定操作
排除区间（7.3）	5%	100%	已恢复
NAS 级持久化（9）	12%	75%	SQLite 持久化；启动时加载视频列表
加权综合完成度：约 93%

✅ 已完成功能（当前版本 v1.2.0）
基础框架与界面
PySide6 桌面应用

综合布局：左侧视频列表 + 右侧主工作区

启动时从数据库加载视频列表（移除硬编码扫描）

视频信息显示：时长、大小

动态分区：3~7 分区自由切换（下拉框选择）

自定义分区：预览窗口中通过分割点自定义分区

网格密度切换：9/12/16/25 张布局

排除区间：手动设置排除时间段 ✅ 已恢复

批量删除：Ctrl+点击 / Shift+点击 多选后批量删除（不删除文件）✅ v1.2 新增

左侧视频列表状态标识（👁️/⭐/✅）

搜索视频：实时过滤视频列表

导入视频：支持文件 + 文件夹导入，基于文件名去重

清理缓存：清理临时截图文件 ✅ v1.2 新增

撤销/重做：针对收藏/锁定操作 ✅ v1.2 新增

截图生成与显示
异步 FFmpeg 提取关键帧

截图自动裁剪片头片尾

截图网格自适应显示（保持宽高比）

每张截图左下角显示时间戳（hh:mm:ss 格式）✅ v1.2 优化

左上角显示截图序号

选中状态：左上角蓝色圆点

锁定状态：🔒 标记

收藏管理（7.6）
收藏/取消收藏选中截图（⭐ 标记）

左侧面板显示收藏总数

查看收藏弹窗（自适应布局，支持最大化）

收藏弹窗中正确显示 ⭐ 和绿色导出圆点

收藏弹窗支持 Zoom 精修入口

收藏弹窗中可收藏候选帧

SQLite 持久化：收藏记录、导出状态完整保存和恢复

锁定功能
锁定/解锁选中截图

刷新未锁定

全部重抽

导出功能
导出选中的截图到 StillPic/视频名/ 目录

导出后截图右下角绿点标记（收藏弹窗内正常）

批量导出收藏截图（收藏弹窗中选中多张导出）

视频片段导出：预览窗口中选段导出到 CoverPicker/Clips/

支持 WMV、FLV、WEBM、AVI、MOV、MKV 等格式自动重编码

Zoom 精修（L1~L4）
L1（初选）：±4 秒范围，9 张候选帧

L2（精选）：±2 秒范围，9 张候选帧

L3（细选）：±1 秒范围，9 张候选帧

L4（定稿）：±0.5 秒范围，9 张候选帧

触发入口：主界面 + 收藏弹窗

交互规则：单击选中 · 双击放大预览 · 按钮执行操作

支持操作：替换原图、收藏、导出、预览、细选进入下一层、全选/取消全选

预览窗口（独立对话框）✅ v1.2 重构
独立可调整大小窗口，彻底解决布局问题

视频帧预览（FFmpeg 提取）

时间轴滑块浏览

时间轴刻度：显示 0%、25%、50%、75%、100% 位置时间标签 ✅ v1.2 新增

片段范围选择（设起始/设结束）

视频片段导出到 CoverPicker/Clips/

自定义分区（分割点模式）：

添加分割点（当前预览位置）

清除所有分割点

应用分区生成自定义分区

分割点列表实时显示

交互操作
单击：选中/取消选中

双击：放大预览（滚轮缩放 + 拖动平移）

视频列表：双击加载视频，单击仅选中（不加载）

全选 / 取消全选

Ctrl+点击 / Shift+点击 多选 ✅ v1.2 新增

状态持久化
_save_state_to_db() 正确计算 is_starred、is_exported、is_viewed

收藏表增量更新

导出状态同步到 favorites.is_exported

分区按钮显示时间范围

视频列表状态图标启动刷新

启动时从数据库加载视频列表 ✅ v1.2 新增

协程任务管理
load_video() 取消之前的加载任务

_load_segment() 正确处理 CancelledError

refresh_unlocked() 和 reset_all() 捕获取消异常

closeEvent() 清理任务

代码架构
控制器分离：segment_controller.py 管理业务逻辑

预览控制器分离：preview_controller.py

自定义控件分离：image_labels.py

对话框分离：favorites_dialog.py, exclude_dialog.py

预览窗口独立：preview_dialog.py ✅ v1.2 新增

🚧 未完成功能（v1.3 规划）
主题跟随（深色/浅色模式）

更多格式支持

🟢 低优先级（v2.0 规划）
独立可执行文件

缓存自动清理机制

✅ 已修复问题
问题	状态	说明
WMV 等格式片段导出自动重编码	✅ 已修复	自动检测格式并重新编码
subprocess UnicodeDecodeError	✅ 已修复	FFmpeg 输出使用 utf-8 编码
收藏弹窗导出后图标不更新	✅ 已修复	正确调用 _save_state_to_db()
自定义分区应用后重复弹窗	✅ 已修复	应用后静默清除分割点
预览面板布局问题	✅ 已修复	改为独立对话框
视频列表持久化	✅ 已修复	启动时从数据库加载
排除区间功能失效	✅ 已修复	已恢复
截图时间戳格式	✅ 已修复	改为 hh:mm:ss
FavImageLabel 不显示	✅ 已修复	添加 set_image_size 方法
📁 当前项目结构
text
C:\CoverPicker
├── main.py
├── requirements.txt
├── .venv/
├── src/
│   ├── controllers/
│   │   ├── __init__.py
│   │   ├── segment_controller.py
│   │   └── preview_controller.py
│   ├── database.py
│   ├── video_scanner.py
│   └── __init__.py
├── ui/
│   ├── widgets/
│   │   ├── __init__.py
│   │   └── image_labels.py
│   ├── dialogs/
│   │   ├── __init__.py
│   │   └── favorites_dialog.py
│   └── views/
│       ├── segment_view.py
│       ├── preview_dialog.py      # 独立预览窗口 ✅
│       ├── preview_panel.py       # 已废弃（保留备份）
│       ├── zoom_dialog.py
│       ├── zoom_preview.py
│       └── exclude_dialog.py
├── StillPic/
├── Clips/
└── docs/
🛠️ 技术栈
组件	版本
Python	3.13
GUI 框架	PySide6 6.11.1
视频处理	FFmpeg / FFprobe
异步框架	asyncio + qasync
数据库	SQLite
📝 开发日志（2026-07-15）
已修复/新增（v1.2.0）
视频列表持久化：启动时从数据库加载所有视频

导入视频：写入数据库，重启后可恢复

清理缓存：删除临时截图文件

撤销/重做：支持收藏/锁定操作的撤销和重做

排除区间：功能恢复，通过对话框设置排除时间段

批量删除：支持 Ctrl+点击 / Shift+点击 多选后批量删除

预览窗口：从固定面板改为独立对话框，彻底解决布局问题

分区数量：从 3~10 改为 3~7

时间戳格式：改为 hh:mm:ss 格式

时间轴刻度：在预览窗口滑块下方显示时间标签

FavImageLabel：修复收藏弹窗图片显示问题

📌 开发协作规则
代码提供方式

完整文件替换：任何时候都必须提供完整文件内容，并在开头注明文件路径，不允许只提供部分代码。
提供完整代码前，不需要列出修改内容1…… 修改内容2……。

改错规则
明确的报错代码（Traceback）：先分析原因，确认后提供完整代码

无明确报错代码（仅文字描述现象）：先与用户确认现象，再分析原因，有必要的话要增加输出日志，确保无误后提供修改好的完整代码。提供修复代码前不需要用户确认原因分析是否正确。

📌 下一阶段计划（v1.3）
主题跟随（深色/浅色模式）

更多格式支持

最后更新： 2026-07-15